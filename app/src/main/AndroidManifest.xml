<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frontend">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FrontEnd">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dashboard_pasien"/>
        <activity android:name=".riwayatObat_pasien"/>
        <activity android:name=".dash_cari"/>
        <activity android:name=".proses_resep"/>
        <activity android:name=".register"/>
        <activity android:name=".register_dokter"/>
        <activity android:name=".dashboard_dokter"/>
        <activity android:name=".form_resep"/>
        <activity android:name=".dokter_editdelete"/>
        <activity android:name=".manage_pasien"/>

    </application>

</manifest>